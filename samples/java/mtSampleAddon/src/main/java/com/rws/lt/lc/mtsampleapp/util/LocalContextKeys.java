package com.rws.lt.lc.mtsampleapp.util;

public enum LocalContextKeys {
    /** this is set by the initiator of a remote call: controller aspect, consuming thread etc. */
    CONVERSATION_CONTEXT,

    RENDER_FLAGS,

    ACTIVE_ACCOUNT_ID
}
